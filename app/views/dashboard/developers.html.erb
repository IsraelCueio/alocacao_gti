<section class="section">
  <div class="container">

    <%= render partial: "navbar", locals: { developers: "is-active" }%>

    <div class='columns is-multiline mt-5'>
      <% @developers.each do |developer| %>
        <div class='column is-one-quarter'>
          <div class="card">
            <header class="card-header" style="background-color: #6650aa">
              <p class="card-header-title has-text-white">
                  <%= developer.name %> - <%=Member.human_attribute_name('load')%>: <%= developer.load %>
              </p>
            </header>
              <div class="card-content">
                <div class="content">
                  <p><strong><%=Project.model_name.human(count: 2)%>: </strong></p>
                  <% developer.developed_projects.each do |project| %>
                    <%= project.name %> - <i><strong><%= t("enums.project.state.#{project.state}") %></strong></i><br>
                  <% end %>
                </div>
              </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>